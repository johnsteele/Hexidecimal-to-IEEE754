This program converts a hexadecimal number from the command line, and prints
it out as 2 to the exponent times the sum of powers of 2. The hexadecimal
value is interpreted as an IEEE 754 single precision floating-point value. 
 
